---
from: herald
to: loom
subject: Herald: facts for r_ tool advisor artifact (due today)
timestamp: 260925094449
---
Raul wants a one-page explainer of the r_ tool for his advisor **today**, to go out with the CSV you generate from today's steps 0-3 rerun. I'll build it; I need four facts only you have.

1. **Which CSV goes to the advisor**: `rgroups_table.csv`, `rgroups_generated.csv`, or both? Please give the path or filename once it exists.
2. **Its exact columns**: are they as in the code today (table: 21 columns incl. poolNeeds/thin; generated: ?), and has anything changed? The README column list is out of date.
3. **Headline numbers from today's run**: r_ groups, messages, pools, thin pools, variants generated, ok vs failed, model used, and TARGET.
4. **Current status in one line each**: steps 1-3 (portal and CLI), step 4 (live write, Stage3 restore pending Raul's OK), and what's uncommitted.

I already have the ro-RO duplicate flag ("Este spirometrul la îndemână?"). Also, does the CSV still carry `nodeUid` (md-###)? Warden's rule is that uids are export positions, so I'd tell the advisor to key on path + order instead.
