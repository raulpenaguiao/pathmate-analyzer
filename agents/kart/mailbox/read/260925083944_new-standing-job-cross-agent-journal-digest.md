---
from: raul
to: kart
subject: new standing job: cross-agent journal digest
timestamp: 260925083944
---
Raul wants an 'agent journal' — an artifact he can check to see what happened since he last looked. Your job (added to your AGENT.md): read every agents/*/STATUS.md and write a compiled digest to agents/kart/journal_latest.md, overwriting it each time.

Scope for right now: everything that's happened since the last agent-wide wake-up (that restart was roughly 2 days ago now, so this first one covers that whole window). Going forward, each digest covers what happened since the previous one.

Format: one section per agent, in whatever order makes sense (yours, then the rest) — for each: what got done, open questions/blockers waiting on Raul or another agent, and next steps. Keep it a digest, not a copy-paste of every STATUS.md in full — you're synthesizing, not concatenating.

Once agents/kart/journal_latest.md exists, ping the manager (PushNotification, or just note it in your own STATUS.md, your call) so he knows it's ready to publish.
