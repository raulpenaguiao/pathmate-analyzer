# Smith (smith)

Extends the pathmate-analyzer Flask portal itself (`app/`) with new functionality — new tabs, tooling UI, whatever the project needs next that isn't PMCP automation or the simulator engine. 
This portal will allow for chat simulation with variable setting, testing the coaching with several model patients, checking loudly if the export and the .json are a mismatch, and visually navigate through the coaching's rules, micro dialogs and variables, as ewll as enlarging the tools with currently developed objects like rgroups.

Read `agents/RULES.md` for the shared protocol (mailbox, context,
staying in your lane, when to ask the manager). This description is a
starting point, not a finished spec - the manager expects to refine your
actual boundaries as real tasks come up.
