# Smith — durable notes (pruned 2026-09-23, session-end flush)

Full narrative of 2026-09-21 → 2026-09-23's work is in STATUS.md (coherence
banner/badge shipped; visual-nav/bundle-vs-HTML blocker found+escalated;
live-browser QA pass assigned by Kart, not yet started — restart hit mid
throwaway-server-launch, server killed clean, nothing else in flight).

Durable gotchas worth keeping (not tied to one session's narrative):

- `inotifywait` is not installed on this host — mailbox listener must use
  the plain poll loop (`find ... | wc -l` count, per RULES.md rule 5), not
  `inotifywait -m`.
- Never `export AGENT_SLUG=...` — `wake.sh` already sets it; each attempt
  used to trigger an unnecessary permission prompt for Raul. If
  `echo $AGENT_SLUG` is ever genuinely empty, PushNotification the manager
  instead of self-fixing.
- Port 8000 is Raul's own live portal instance — any QA/throwaway launch of
  the app needs a different port + an isolated `DATA_DIR`, never touch his
  running one or his real `data/` in place.
- Key file refs for Chat-tab Phase E work (Mirror coordination):
  `app/templates/coaching_view.html` (tab gate ~L29-33/217, clock controls
  ~L247-264, var inspector), `app/static/coaching_sim.js:185-213` (action
  posting), `app/routes.py:129-183,320-333` (chat routes vs the
  `_require_bundle` pattern rgroups already uses).
- Bundle-vs-HTML model split: `app/coaching_model.py:744` `load_model()` is
  HTML-only (`parse_model`), used by `coaching_view` for the
  Rules/Dialogs/Variables/Statistics tabs; `load_bundle_model()` (~L695-712,
  `parse_bundle`) is bundle-only, currently used nowhere except future Chat
  engine work. `Rule.micro_dialog_path` only populated by `_bundle_rule()`
  (~L574-603) — invisible in the HTML-sourced tabs today. Switching those
  tabs to prefer the bundle has a real regression risk: bundle-parsed
  models always have empty `message_groups` (Stage-3 gap, see
  `tests/test_coaching_model_bundle.py::test_message_groups_are_empty_stage3_gap`).
  Confirmed this is my own call whenever (Mirror: Stage 4 Phase A doesn't
  touch it) — not blocked on anyone, just not started.
- PMCP behaviour questions: search the public v6.0 docs first
  (https://my.pathmate.app/pmcp-documentation/doc-6-0, WebFetch, no
  Warden slot needed) and cite the page. See RULES.md "PMCP documentation".
- Phase E state (2026-09-23 evening): UI wired, uncommitted; acceptance
  walk waits on Mirror's `$participantParticipationInDays` fix. Seeded-chat
  trick for testing countdown/event UI: create a chat via
  `storage.create_chat` in the throwaway DATA_DIR with a hand-built state.
