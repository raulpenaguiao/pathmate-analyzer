# Smith durable notes, 2026-09-24
- md-NNN / md-NNN#NNN / rule uids are positions within ONE export's sweep.
  Never compare or persist them across exports, and never use them in notes.
  Use the dialog's menu `path` (events carry `dialog_menu_path`). Raul's rule.
- Chat staleness: `state["model_fingerprint"]` vs `_model_fingerprint(model)`
  (routes `_fingerprint_status`). Bundle replacement in the UI = Detach + Attach.
- Test exports: `data/exports/..._20260917-174650_jumps.json` is complete +
  depth + jumps. The 0918 one is a partial sweep. Both still have the one-line
  medication options (patch in the throwaway copy) until a fresh export.
