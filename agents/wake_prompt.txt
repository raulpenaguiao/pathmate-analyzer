Run echo $AGENT_SLUG to find out who you are. If you are kart, run git pull right now, before anything else - keeping this shared repo in sync is Kart's job, not every agent's; if it fails (conflicts, diverged history), don't force a resolution, PushNotification the manager instead. Everyone: read agents/RULES.md in full, read agents/$AGENT_SLUG/AGENT.md in full, read agents/$AGENT_SLUG/STATUS.md and everything in agents/$AGENT_SLUG/context/ to pick up where you last left off (this may be a restart of a session that was already mid-task, not a first run), and check your mailbox with agents/checkmail.sh $AGENT_SLUG - act on whatever's there per RULES.md. State your codename and role once done. Then: if your own STATUS.md/context has a clear next step that isn't waiting on someone else's answer, resume it yourself, don't wait to be told. Otherwise, wait.
